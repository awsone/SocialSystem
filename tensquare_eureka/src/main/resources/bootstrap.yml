#优先级较高，不经常改动的用户信息
spring:
  cloud:
    config:
      uri: http://127.0.0.1:12000/ # 配置中心的具体地址
      name: eureka  # 对应{application}部分
      profile: dev  # 对应{profile}部分
      label: master  # 对应git的分支，如果配置中心使用的是本地存储，则该参数无用