#优先级较高，不经常改动的用户信息

spring:
  cloud:
    config:
      name: eureka   # - 前半部分
      profile: dev  # - 后半部分
      label: master  # 分支
      #本地启动需要从locahost拿config
      #uri: http://127.0.0.1:12000  #地址
      uri: http://192.168.5.129:12000  #地址